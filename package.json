{
  "name": "microshield-bpo-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static MicroShield BPO website",
  "scripts": {
    "pretest": "npm run build",
    "build": "node scripts/build.js",
    "test": "node scripts/test.js"
  }
}
